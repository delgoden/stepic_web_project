server {

    #access_log /var/log/nginx.access_log simple;

    listen 80 default;

    location ^~ /uploads/ {
        root /home/delgo/web;
    }

    location ~* ^.+\.\w+$ {
        root /home/delgo/web/public;
    }

    location / {
        return 404;
    }

}